<div class='song_show_items'><h1><%= @category.name %></h1><ul>
<p><% @category.movies.order(title: :asc).each do |c| %>
	<li><%= link_to(c.title, movie_path(c.id)) %></li><br>
<% end %>
</p></ul></div>